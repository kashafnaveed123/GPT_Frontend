// // src/App.js
// import React, { useState } from "react";
// import { Sparkles } from "lucide-react";
// import { AuthProvider, useAuth } from "./context/AuthContext";
// import Login from "./auth/Login";
// import Signup from "./auth/Signup";
// import ChatbotUI from "./components/ChatbotUI";

// function AppContent() {
//   const { user, isLoading } = useAuth();
//   const [authMode, setAuthMode] = useState("login");

//   if (isLoading) {
//     return (
//       <div 
//         className="min-h-screen flex items-center justify-center" 
//         style={{
//           background: "linear-gradient(135deg, #FCE7F3 0%, #F3E8FF 100%)",
//         }}
//       >
//         <div className="text-blue-600 text-xl font-semibold flex items-center gap-3">
//           <Sparkles className="animate-pulse" size={24} />
//           Loading...
//         </div>
//       </div>
//     );
//   }

//   if (!user) {
//     return authMode === "login" ? (
//       <Login onSwitchToSignup={() => setAuthMode("signup")} />
//     ) : (
//       <Signup onSwitchToLogin={() => setAuthMode("login")} />
//     );
//   }

//   return <ChatbotUI />;
// }

// export default function App() {
//   return (
//     <AuthProvider>
//       <AppContent />
//     </AuthProvider>
//   );
// }



// import React, { useEffect, useRef, useState } from "react";
// import { Send, Paperclip, Mic, RefreshCw, Menu } from "lucide-react";
// import { motion, AnimatePresence } from "framer-motion";
// import ReactMarkdown from "react-markdown";
// import remarkGfm from "remark-gfm";

// const COLORS = {
//   primary: "#1E40AF", 
//   accent: "#2563EB",  
//   bg: "#FDF2F8",      
//   card: "#FFFFFF",    
// };

// // Sidebar Component
// function Sidebar({ 
//   isOpen, 
//   onToggle, 
//   chatHistory = [], 
//   currentChatId,
//   onSelectChat,
//   onNewChat,
//   onDeleteChat,
//   onLogout,
//   userName = "User"
// }) {
//   const [hoveredChat, setHoveredChat] = useState(null);

//   const groupChatsByDate = () => {
//     const today = new Date();
//     const yesterday = new Date(today);
//     yesterday.setDate(yesterday.getDate() - 1);
//     const weekAgo = new Date(today);
//     weekAgo.setDate(weekAgo.getDate() - 7);

//     const groups = { today: [], yesterday: [], week: [], older: [] };

//     chatHistory.forEach(chat => {
//       const chatDate = new Date(chat.date);
//       const dateStr = chatDate.toDateString();
      
//       if (dateStr === today.toDateString()) groups.today.push(chat);
//       else if (dateStr === yesterday.toDateString()) groups.yesterday.push(chat);
//       else if (chatDate > weekAgo) groups.week.push(chat);
//       else groups.older.push(chat);
//     });

//     return groups;
//   };

//   const groups = groupChatsByDate();

//   const ChatGroup = ({ title, chats }) => {
//     if (chats.length === 0) return null;
//     return (
//       <div className="mb-4">
//         <h3 className="text-xs font-semibold text-blue-400 uppercase tracking-wide px-4 mb-2">
//           {title}
//         </h3>
//         {chats.map(chat => (
//           <div
//             key={chat.id}
//             className={`relative group mx-2 rounded-lg transition-all cursor-pointer ${
//               currentChatId === chat.id 
//                 ? 'bg-gradient-to-r from-blue-600 to-indigo-700 text-white' 
//                 : 'hover:bg-blue-50 text-gray-700'
//             }`}
//             onClick={() => onSelectChat(chat.id)}
//             onMouseEnter={() => setHoveredChat(chat.id)}
//             onMouseLeave={() => setHoveredChat(null)}
//           >
//             <div className="flex items-center gap-3 px-4 py-3">
//               <div className="flex-1 min-w-0">
//                 <p className="text-sm font-medium truncate">{chat.title || 'Untitled'}</p>
//                 <p className={`text-xs truncate ${currentChatId === chat.id ? 'text-blue-100' : 'text-gray-500'}`}>
//                   {chat.preview || 'No messages'}
//                 </p>
//               </div>
//               {hoveredChat === chat.id && (
//                 <button
//                   onClick={(e) => { e.stopPropagation(); onDeleteChat(chat.id); }}
//                   className="p-1 rounded hover:bg-blue-200"
//                 >
//                   🗑️
//                 </button>
//               )}
//             </div>
//           </div>
//         ))}
//       </div>
//     );
//   };

//   return (
//     <>
//       {isOpen && (
//         <div className="fixed inset-0 bg-black/30 z-40 lg:hidden" onClick={onToggle} />
//       )}
//       <div
//         className={`fixed top-0 left-0 h-full z-50 transform transition-transform duration-300 ${
//           isOpen ? 'translate-x-0' : '-translate-x-full'
//         } lg:translate-x-0 lg:sticky lg:top-0`}
//         style={{ width: '280px' }}
//       >
//         <div className="h-screen flex flex-col bg-white shadow-2xl border-r border-blue-100">
//           <div className="p-4 border-b border-blue-100">
//             <div className="flex items-center justify-between mb-4">
//               <div className="flex items-center gap-3">
//                 <div
//                   className="w-10 h-10 rounded-full flex items-center justify-center text-white font-bold"
//                   style={{ background: "linear-gradient(135deg, #2563EB, #1E40AF)" }}
//                 >
//                   {userName.charAt(0).toUpperCase()}
//                 </div>
//                 <div>
//                   <p className="text-sm font-semibold text-gray-800">{userName}</p>
//                   <p className="text-xs text-blue-500">Online</p>
//                 </div>
//               </div>
//               <button onClick={onToggle} className="lg:hidden p-2 hover:bg-blue-50 rounded-lg">
//                 ✕
//               </button>
//             </div>
//             <button
//               onClick={() => { onNewChat(); if (window.innerWidth < 1024) onToggle(); }}
//               className="w-full flex items-center justify-center gap-2 px-4 py-3 rounded-lg text-sm font-semibold text-white shadow-md"
//               style={{ background: "linear-gradient(135deg, #2563EB, #1E40AF)" }}
//             >
//               ➕ New Chat
//             </button>
//           </div>
//           <div className="flex-1 overflow-y-auto py-4">
//             {chatHistory.length === 0 ? (
//               <div className="px-4 py-8 text-center">
//                 <p className="text-sm text-gray-500">No chat history yet</p>
//               </div>
//             ) : (
//               <>
//                 <ChatGroup title="Today" chats={groups.today} />
//                 <ChatGroup title="Yesterday" chats={groups.yesterday} />
//                 <ChatGroup title="Previous 7 Days" chats={groups.week} />
//                 <ChatGroup title="Older" chats={groups.older} />
//               </>
//             )}
//           </div>
//           <div className="p-4 border-t border-blue-100">
//             <button
//               onClick={onLogout}
//               className="w-full flex items-center justify-center gap-2 px-4 py-3 rounded-lg text-sm font-semibold text-white shadow-md"
//               style={{ background: "linear-gradient(135deg, #F43F5E, #DC2626)" }}
//             >
//               🚪 Logout
//             </button>
//           </div>
//         </div>
//       </div>
//     </>
//   );
// }

// export default function ChatbotUI({
//   onSend,
//   title = "Kashaf GPT",
//   subtitle = "Professional Mern AI Developer",
//   userName = "Guest User",
//   onLogout
// }) {
//   const [messages, setMessages] = useState([
//     { id: 1, who: "bot", text: "🎀 Hello! I'm Kashaf's Bot — AI assistant to introduce kashaf's tech. How can I support you today?", time: new Date().toISOString() },
//   ]);
//   const [input, setInput] = useState("");
//   const [isSending, setIsSending] = useState(false);
//   const [sidebarOpen, setSidebarOpen] = useState(false);
//   const [chatHistory, setChatHistory] = useState([]);
//   const [currentChatId, setCurrentChatId] = useState(null);
//   const endRef = useRef(null);

//   useEffect(() => {
//     const saved = localStorage.getItem('chatHistory');
//     if (saved) setChatHistory(JSON.parse(saved));
//   }, []);

//   useEffect(() => {
//     if (chatHistory.length > 0) localStorage.setItem('chatHistory', JSON.stringify(chatHistory));
//   }, [chatHistory]);

//   useEffect(() => {
//     endRef.current?.scrollIntoView({ behavior: "smooth" });
//   }, [messages]);

//   async function handleSend(e) {
//     e?.preventDefault();
//     const text = input.trim();
//     if (!text || isSending) return;

//     const userMsg = { id: Date.now(), who: "user", text, time: new Date().toISOString() };
//     setMessages((m) => [...m, userMsg]);
//     setInput("");
//     setIsSending(true);

//     try {
//       const response = await onSend(text);
//       const botMsg = {
//         id: Date.now() + 1,
//         who: "bot",
//         text: response?.text ?? "(no response)",
//         meta: response?.sources ?? [],
//         time: new Date().toISOString(),
//       };
//       setMessages((m) => [...m, botMsg]);
//       saveCurrentChat(text, botMsg.text);
//     } catch (err) {
//       setMessages((m) => [...m, { id: Date.now() + 2, who: "bot", text: `⚠️ Error: ${err.message || err}` }]);
//     } finally {
//       setIsSending(false);
//     }
//   }

//   function saveCurrentChat(userText, botText) {
//     if (!currentChatId) {
//       const newChat = {
//         id: 'chat_' + Date.now(),
//         title: userText.substring(0, 40) + (userText.length > 40 ? '...' : ''),
//         preview: botText.substring(0, 60) + (botText.length > 60 ? '...' : ''),
//         date: new Date().toISOString(),
//         messages: [...messages, 
//           { id: Date.now(), who: "user", text: userText, time: new Date().toISOString() },
//           { id: Date.now() + 1, who: "bot", text: botText, time: new Date().toISOString() }
//         ]
//       };
//       setCurrentChatId(newChat.id);
//       setChatHistory(prev => [newChat, ...prev]);
//     } else {
//       setChatHistory(prev => prev.map(chat => 
//         chat.id === currentChatId ? { ...chat, messages: [...messages], preview: botText.substring(0, 60) } : chat
//       ));
//     }
//   }

//   function handleNewChat() {
//     setCurrentChatId(null);
//     setMessages([{ id: Date.now(), who: "bot", text: "🎀 Hello! I'm Kashaf's Bot. How can I help?", time: new Date().toISOString() }]);
//   }

//   function handleSelectChat(chatId) {
//     const chat = chatHistory.find(c => c.id === chatId);
//     if (chat) { setCurrentChatId(chatId); setMessages(chat.messages); }
//   }

//   function handleDeleteChat(chatId) {
//     if (window.confirm('Delete this chat?')) {
//       setChatHistory(prev => prev.filter(c => c.id !== chatId));
//       if (currentChatId === chatId) handleNewChat();
//     }
//   }

//   function handleLogout() {
//     if (window.confirm('Logout?')) {
//       localStorage.removeItem('authToken');
//       if (onLogout) onLogout();
//     }
//   }

//   return (
//     <div className="flex h-screen overflow-hidden">
//       <Sidebar
//         isOpen={sidebarOpen}
//         onToggle={() => setSidebarOpen(!sidebarOpen)}
//         chatHistory={chatHistory}
//         currentChatId={currentChatId}
//         onSelectChat={handleSelectChat}
//         onNewChat={handleNewChat}
//         onDeleteChat={handleDeleteChat}
//         onLogout={handleLogout}
//         userName={userName}
//       />
//       <div className="flex-1 flex items-start justify-center p-4 lg:p-6 overflow-auto" style={{ background: "linear-gradient(135deg, #FCE7F3 0%, #F3E8FF 100%" }}>
//         <div className="w-full max-w-4xl">
//           <div className="rounded-xl shadow-2xl overflow-hidden" style={{ background: COLORS.card }}>
//             <div className="flex items-center justify-between px-4 lg:px-6 py-4 border-b border-blue-100">
//               <div className="flex items-center gap-3">
//                 <button onClick={() => setSidebarOpen(!sidebarOpen)} className="lg:hidden p-2 hover:bg-blue-50 rounded-lg">
//                   <Menu size={20} />
//                 </button>
//                 <div className="w-10 h-10 rounded-xl flex items-center justify-center text-white font-bold" style={{ background: "linear-gradient(135deg, #2563EB, #1E40AF)" }}>K</div>
//                 <div>
//                   <div className="text-base font-semibold">{title}</div>
//                   <div className="text-xs text-blue-500">{subtitle}</div>
//                 </div>
//               </div>
//               <button onClick={handleNewChat} className="px-3 py-2 rounded-lg text-sm font-medium text-white" style={{ background: "linear-gradient(135deg, #2563EB, #1E40AF)" }}>
//                 <RefreshCw size={16} className="inline mr-1" /> New
//               </button>
//             </div>
//             <div className="h-[calc(100vh-280px)] overflow-y-auto p-4 space-y-4 bg-gradient-to-b from-white to-blue-50/60">
//               <AnimatePresence>
//                 {messages.map((m) => (
//                   <motion.div key={m.id} initial={{ opacity: 0, y: 6 }} animate={{ opacity: 1, y: 0 }} exit={{ opacity: 0, y: -6 }}>
//                     {m.who === "user" ? (
//                       <div className="flex justify-end">
//                         <div className="max-w-[85%] px-4 py-2 rounded-xl text-sm text-white" style={{ background: "linear-gradient(135deg, #1E40AF, #2563EB)" }}>{m.text}</div>
//                       </div>
//                     ) : (
//                       <div className="flex gap-2">
//                         <div className="w-8 h-8 rounded-full flex items-center justify-center text-white font-medium" style={{ background: "linear-gradient(135deg, #2563EB, #1E40AF)" }}>K</div>
//                         <div className="max-w-[85%] px-4 py-3 rounded-xl text-sm bg-blue-50">
//                           <ReactMarkdown remarkPlugins={[remarkGfm]}>{m.text}</ReactMarkdown>
//                           {m.meta?.length > 0 && <div className="mt-2 text-xs text-blue-600">Sources: {m.meta.slice(0, 3).join(", ")}</div>}
//                         </div>
//                       </div>
//                     )}
//                   </motion.div>
//                 ))}
//               </AnimatePresence>
//               <div ref={endRef} />
//             </div>
//             <div className="px-4 py-4 border-t flex gap-2">
//               <input value={input} onChange={(e) => setInput(e.target.value)} onKeyPress={(e) => e.key === 'Enter' && handleSend()} placeholder="Ask anything..." className="flex-1 px-4 py-2 rounded-lg border border-blue-100 bg-blue-50 outline-none" />
//               <button onClick={handleSend} disabled={isSending} className="px-6 py-2 rounded-lg text-white font-semibold" style={{ background: "linear-gradient(135deg, #2563EB, #1E40AF)" }}>
//                 {isSending ? "..." : <Send size={16} />}
//               </button>
//             </div>
//           </div>
//         </div>
//       </div>
//     </div>
//   );
// }